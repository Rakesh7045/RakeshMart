# Copy this file to config.properties (which is .gitignore'd) and fill in real values.
# NEVER commit config.properties with real credentials.

db.url=jdbc:h2:tcp://localhost:9092/./data/rakeshmart
db.user=sa
db.password=
db.pool.size=10

# ai.chatbot.provider = mock | gemini
ai.chatbot.provider=mock
