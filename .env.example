# Copy to .env locally / set as real environment variables on your deployment platform.
# GEMINI_API_KEY is read server-side only by GeminiChatProvider - never expose it to the browser.
GEMINI_API_KEY=
DB_URL=jdbc:h2:tcp://localhost:9092/./data/rakeshmart
DB_USER=sa
DB_PASSWORD=
AI_CHATBOT_PROVIDER=mock
